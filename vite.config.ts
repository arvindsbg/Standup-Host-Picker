import { defineConfig } from 'vite';

export default defineConfig({
  base: '/Standup-Host-Picker/',
  plugins,
  resolve: {
    // Your resolve configurations here
  },
  // Other configurations here
});